Program:
    match=Match
;

Match:
    'match' '(' type=MatchType expr=Expression ')' '{'
        cases*=Case
    '}'
;

Case:
    'case' '[' pats*=Expression[','] ']' block=Block
;

Expression: ID | NUMBER;

MatchType:
    ID | ID '*'
;

Block:
    '{' /.*/ '}'
;
