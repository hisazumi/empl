Program:
    match=Match
;

Match:
    'match' '(' type=MatchType expr=ID ')' '{'
        cases*=Case
    '}'
;

Case:
    'case' pat=Pattern block=Block
;

Expression: ID | NUMBER | (Op ID) | (Op NUMBER) | '_';

Op: '<=' | '>=' | '>' | '<';

Pattern:
    ('[' pats*=Pattern[','] ']') | expr=Expression
;

MatchType:
    ID | ID '*'
;

Block:
    '{' /.*/ '}'
;
