Program:
    match=Match
;

Match:
    'match' '(' type=ID expr=Expression ')' '{'
        cases*=Case
    '}'
;

Case:
    'case' '[' pats*=Expression[','] ']' block=Block
;

Expression: ID | NUMBER;

Block:
    '{' /.*/ '}'
;
